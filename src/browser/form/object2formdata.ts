// // 작동 안함.개발 안함.
// /**
//  * object转formdata
//  * @param {string} str - string
//  * @param {string} type - all, upper, lower
//  * @returns {FormData}
//  */

// const parsingObj = (object: any) => {
//   // 배열일떄
//   if (Array.isArray(object)) {
//   }
//   // Object일때
//   if (object && !Array.isArray(object) 
//   && typeof object === 'object' && !(object instanceof File)) {
//   }

//   // string, number일때
// };

// export default (object: any): FormData => {
//   const formData = new FormData();
//   return formData;
// };
